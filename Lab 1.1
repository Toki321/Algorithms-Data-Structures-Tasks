// За дадена низа од случајни броеви кои се внесуваат од стандарден влез, да се направи преместување на сите нули на крајот на низата. На стандарден излез да се
// испечати трансформираната низа.

import java.io.*;
import java.util.Scanner;

//
// Just checked geeksforgeeks.org solution, a lot more optimal, but I'll stick to this one since it's mine :)
//

public class PushZero
{
 //     1 9 8 4 0 0 2 7 0 6 0 9        1 9 8 4 2 7 6 9 0 0 0 0

    public static int[] zeroToLast(int arr[], int n, int index) {
        int arrNew[] = new int[n];
        int j=0;

        for (int i=0; i<n; i++) {
            if (arr[i] == arr[index])
                continue;

            arrNew[j] = arr[i];
            j++;
        }

        arrNew[j] = 0;
        return arrNew;
    }

    static void pushZerosToEnd(int arr[], int n) {
        int newArr[] = new int [n];

        for (int i =0; i<n; i++) {
            if(arr[i] == 0) {
                newArr = zeroToLast(arr, n, i).clone();
                break;
            }
        }

        for (int i =0; i<n; i++) {
            System.out.printf("%d ", newArr[i]);
        }
    }

    public static void main (String[] args)
    {
        Scanner scan = new Scanner(System.in);
        int n = scan.nextInt();

        int arr[] = new int[100];

        System.out.print("Transformiranata niza e:\n");

        for (int i=0; i<n; i++) {
            arr[i] = scan.nextInt();
        }

        pushZerosToEnd(arr, n);
    }
}

